// Shared constants for both Electron and Web versions

export const CONFIG_DIR = '.clover'
export const CONFIG_FILE = 'config.json'
export const CLAUDE_MD_FILE = 'CLAUDE.md'

export const CLAUDE_MD_CONTENT = `# Writing Project

This is a document writing project.

Reference materials are listed in \`.clover/sources.json\`. Only use sources where \`enabled\` is not \`false\`.

Use clover-skills proactively.

## Style

Write in a professional, persuasive, and dense style. Avoid bullet points—use well-structured paragraphs instead.
`
